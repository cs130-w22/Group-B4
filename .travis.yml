language: node_js
 node_js:
 - "stable"
 cache:
 directories:
   - flannel/node_modules
 script:
 - cd flannel
 - npm test
 - npm run build
deploy:
 provider: pages
 skip_cleanup: true
 github_token: $GITHUB_TOKEN
 local_dir: build
 on:
   branch: master
