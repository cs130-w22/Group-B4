const {react} = require('react');
const {LogIn} = require('../src/pages/Login')
describe('testing login and signup react functional components', () => {
    it('loginUiTests', () => {
        done();
    })
})
